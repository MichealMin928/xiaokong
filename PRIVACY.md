# 隐私说明

适用范围：本仓库构建的 Android App。GitHub、浏览器、Android 系统及第三方 App 遵循各自规则。

## 在手机内处理

App 的发布清单移除了 `INTERNET` 和 `ACCESS_NETWORK_STATE`。相机和麦克风用于本机识别，原始帧与声音只在内存中处理，不写入音视频文件。当前版本没有账号、广告、云端转写或遥测服务器。

无障碍服务用于前台窗口判断和执行操作。仅在需要按钮/分区选择等操作时临时读取可见控件文字、属性和位置；候选过期、页面改变或结束选择后清除。安全窗口和第三方页面可能不允许读取或操作。

## 本机保存及导出

- 设置与校准保存在应用私有目录。
- 最近固定语音指令及其处理结果最多保留 80 条，可查看和清空；不保存任意对话转写。
- 主动填写的反馈与跟读测试最多保留 100 条，包括用户输入说明、版本、环境标签、配置及运行指标。
- 只有进入动作采集并主动开始时才记录关键点、时间和判定结果，不存储摄像头视频。

反馈可以查看、删除或主动导出。导出的文件不再只受 App 私有目录保护，说明文字可能包含用户自行填写的个人信息，请检查后再分享。卸载会删除应用私有数据，外部导出文件需要自行删除。应用禁用系统备份。

## 社区链接

“社区反馈”和“查看源码”交给外部浏览器打开公开 GitHub 页面，不把本机报告、设备信息或识别记录放入 URL，不自动上传。浏览器需要联网；提交到公开 Issue 的内容任何人都可能看到。

公开源码不包含内部真人动作记录、音频、系统日志、设备序列号、个人配置及签名私钥。第三方模型下载发生在开发机上，见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。

## 停止与权限

首页和通知可结束控制；锁屏或退出控制任务会请求停止采集并释放资源。可以随时在系统设置中撤销相机、麦克风、悬浮窗或无障碍权限。不同厂商后台策略仍需验证，异常请报告机型和复现步骤。
